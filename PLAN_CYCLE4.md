# TINProject — 사이클 4 작업 순서

사용자 지정 순서대로 한 모듈씩 구현하고, 각 모듈의 계약·상태·실입력·저장 복원을 검증한 뒤 다음 모듈로 넘어간다.

## 기준 레퍼런스와 변형 경계

| 순서 | 모듈 | 기준 레퍼런스 | 가져올 핵심 루프 | TINProject의 변형 경계 |
|---:|---|---|---|---|
| 01 | `rule_rewriting` | *Baba Is You* | 규칙 조각을 재배열해 공간의 판정을 바꾼다 | 두 규칙을 관찰·수정·재판정하는 신호실. 원작의 문구·캐릭터·자산을 복제하지 않는다 |
| 02 | `dedution_casework` | *The Case of the Golden Idol* | 현장 단서에서 사건의 인과를 재구성한다 | 새 사건과 독자 증거를 사용하고 원작의 사건·문구·자산을 복제하지 않는다 |
| 03 | `physics_toolbox` | *Human: Fall Flat* | 물리 도구와 환경 반응을 실험한다 | 저사양 2D/단순 3D 도구 상호작용으로 제한하며 정밀 타이밍·전투를 만들지 않는다 |
| 04 | `time_loop` | *Outer Wilds* | 반복 관찰로 다음 시도의 지식을 얻는다 | 짧은 지역 루프와 모듈 내부 기록만 사용하며 원작의 우주·서사·자산을 복제하지 않는다 |

## 완료 게이트

- 모듈 계약 6메서드와 `requested` 시그널을 지킨다.
- 관찰/규칙 학습, 상태 변화, 복수 판정 또는 재검토, 오답, 저장 복원 경로를 테스트한다.
- 모듈은 다른 모듈과 앱 구현을 직접 참조하지 않는다. 앱 라우팅은 `app_root.gd`가 소유한다.
- 각 순서의 모듈을 완료한 뒤 고정 4단계 검증과 영향 장면 확인을 통과해야 다음 순서로 이동한다.

## 진행 상태

- 01 `rule_rewriting`: 완료
- 02 `dedution_casework`: 완료
- 03 `physics_toolbox`: 완료
- 04 `time_loop`: 완료
