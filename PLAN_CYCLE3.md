# TINProject — 사이클 3 진행 기록

작성: 2026-09-18. 목표는 신규 5~10개 배치와 서로 다른 세트를 가로지르는 지식 재사용이다.

## 진행 1 — 숫자 없는 시계방

- `teacup_orbit`에서 세 바퀴 뒤 X로 발견하는 측면 공간이다.
- ○·◇·△ 시곗바늘을 아래→왼쪽→위로 기울이면 `signal_desk`로 돌아간다.
- 해법은 사이클 2의 `glyph_gallery`에서 배운 규칙을 그대로 재사용한다.
- 별도 해금·지식 플래그는 없으며 신선한 저장 상태에서도 정답을 바로 실행할 수 있다.
- 오답은 로컬 실패 횟수만 남기고 입력을 초기화한다. 실행 난도나 반복 벌점은 없다.

## 진행 2 — 그림자 나룻배

- `numberless_clock`에서 입력이 비어 있을 때 X로 들어가는 정적 3D 측면 공간이다.
- 세 선착장의 그림자 길이를 보고 가장 긴 가운데 그림자를 선택해 건넌다.
- 좌우 선택과 확인만 사용하며 시간 제한·정밀 조작·재화·해금 플래그가 없다.
- 오답은 로컬 시도 횟수만 남기고, X로 시계방에 즉시 돌아갈 수 있다.

## 진행 3 — 영수증 과수원

- `shadow_ferry`의 정답 출구 뒤에 연결된 2D 종이 과수원이다.
- 세 종이 열매의 글자를 영수증 단서인 비→차→달 순서로 맞춘다.
- 선택·글자 변경·확인만 사용하며 오답은 로컬 실패 횟수만 남긴다.
- X로 나룻배에 돌아갈 수 있고 정답 뒤에는 `signal_desk`로 귀환한다.

## 진행 4 — 틀린 일기예보

- `receipt_orchard` 뒤에 연결된 2D 기상 방송실이다.
- 웅덩이에서 천장으로 오르는 물방울을 보고 “위로 내리는 비”를 고른다.
- 좌우 선택과 확인만 사용하고, 오답 송출은 로컬 횟수 외 불이익이 없다.
- X로 과수원에 돌아가며 정답 뒤에는 `signal_desk`로 귀환한다.

## 진행 5 — 조용한 사물함

- `wrong_weather` 뒤에 연결된 전투·퍼즐 없는 짧은 탐색 공간이다.
- 세 사물함을 어느 순서로든 살피며 찻잔, 준호의 쪽지, 비어 있는 자리를 발견한다.
- 아이템 획득이나 해금은 없고 관찰 사실만 기록된다.
- 세 곳을 모두 본 뒤 `signal_desk`로 돌아가며 X로 방송실에 되돌아갈 수 있다.

## 진행 6 — 기억 세관

- `quiet_locker` 뒤에 연결된 2×2 통과표 선택 공간이다.
- 몸·기억·우산·찻잔 중 몸과 기억에만 도장을 찍으면 문턱이 열린다.
- 오답 선택은 언제든 취소할 수 있고 벌점·아이템 소비·해금 플래그가 없다.
- 프로젝트의 몸+기억 경계 규칙을 세계 내부 문구와 실제 선택으로 보여준다.

## 진행 7 — 종이달 진료소

- `memory_customs` 뒤에 연결된 순서 자유 진찰 공간이다.
- 종이달의 세 증상 카드를 모두 살피면 기록실로 돌아간다.
- 첫 진찰마다 서로 다른 관찰 사실을 남기며 전투·타이밍·아이템 획득은 없다.
- Antigravity MCP에 동일 모듈 폴더를 두 번 위임했으나 응답과 파일 변경이 모두 없어 규칙대로 로컬 구현했다.

## 진행 8 — 잔상 수족관

- `paper_moon_clinic` 뒤에 연결된 세 수조 관찰 퍼즐이다.
- 물고기와 잔상이 반대 방향인 가운데 수조를 고르면 기록실로 돌아간다.
- 오답은 로컬 확인 횟수만 남기며 타이밍·정밀 조작·아이템은 없다.
- Antigravity MCP에 전체 구현과 축소 구현을 각각 위임했으나 둘 다 빈 응답·파일 없음으로 끝나 로컬 구현했다.

## 진행 9 — 종이등대

- `afterimage_aquarium` 뒤에 연결된 세 등대 관찰 선택 공간이다.
- 빛보다 그림자를 먼저 보고 자기 그림자를 비추는 왼쪽 등대를 고르면 `signal_desk`로 귀환한다.
- 좌우 선택과 확인만 사용하며 오답은 로컬 확인 횟수만 남긴다.
- 1152×720 GL Compatibility 캡처에서 세 등대·빛·그림자·선택 강조·입력 안내가 겹치지 않음을 확인했다.
- 사용자가 단독 작업을 지시한 뒤 Antigravity MCP나 서브에이전트 없이 직접 구현·검증했다.

## 진행 10 — 분실된 신호

- `paper_lighthouse` 뒤에 연결된 비주얼 노벨형 단편이다.
- 기록과 준호의 대화, 두 단계의 갈림 선택, 대화 진행·히스토리·관찰 기록·저장을 한 화면 루프로 묶었다.
- 두 선택은 신뢰 수치와 마지막 답장의 문장을 바꾸며 전역 플래그나 공용 성장 재화는 만들지 않았다.
- 1152×720 GL Compatibility 캡처에서 인물 카드와 대화 상자, 선택이 분리되어 보임을 확인했다.

## 진행 11 — 자정의 보랏빛 사건

- `lost_signal_vn` 뒤에 연결된 독자적인 추리 조사 모듈이다.
- 신호 열쇠·잠긴 창문·찢긴 이름표를 조사하고, 범인·수법·동기 세 칸을 증거에 맞춰 채운다.
- 오답 횟수는 모듈 내부에만 남고, 정답 뒤에는 사건 재구성 화면에서 세 답을 확인한 뒤 직접 닫아 `signal_desk` 귀환을 요청한다.
- 1152×720 GL Compatibility 캡처에서 현장 카드·기록 노트·입력 안내를 확인했다.
- 1단계의 완결형 인디 모듈 품질 기준을 2단계의 추리 모듈에 확장했으며, 관련 영감은 `docs/GODOT_INTEGRATIONS.md`에 출처와 함께 기록했다.

## 진행 12 — 신호가 지나간 뒤

- `violet_case`에서 사건을 재구성한 뒤 이어지는 짧은 후일담 모듈이다.
- 접힌 답장·비어 있는 우편함·새벽 창문의 세 흔적을 순서 자유로 읽고, 모두 확인한 뒤 다음 주소를 열어 `signal_desk`로 돌아간다.
- 각 흔적과 최종 문장은 기록에 한 번씩만 남으며, 모듈 저장 상태는 선택 위치와 확인 여부만 가진다.
- 1152×720 GL Compatibility 캡처에서 봉투 영역·세 카드·입력 안내·상태 문구의 분리를 확인했다.

## 검증 현황

- 누적 규모: 신규 24개 + 데모 3개 = 27개
- 통합 러너: 639/639 checks
- GUT: 110/110, 4,295 assertions
- 사이클 3 신규 12모듈 1152×720 GL Compatibility 캡처 확인 완료
- 사이클 3 목표 배치 5~10개를 초과 달성. 다음 작업은 기존 경험의 연결감·시각 품질 또는 통합 팩 중 가장 작은 검증 가능한 산출물로 이어간다.
