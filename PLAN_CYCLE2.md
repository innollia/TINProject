# TINProject — 사이클 2 실행 기록

작성: 2026-09-18. `PROJECT_DECISIONS.md`의 누적 15~20개, 사이클당 5~10개 증산, 지식 플레이 검증을 기준으로 실행했다.

## 목표

> 외부 자산과 새 공용 시스템 없이 6개 모듈을 추가해 누적 15개를 만들고, 관찰한 규칙으로 신선한 상태의 퍼즐과 지름길을 통과하는 두 지식 흐름을 증명한다.

## 신규 세트 「접힌 오후」

| 모듈 | 등급 | 경험 |
|---|---|---|
| `glyph_gallery` | M | ○=아래, ◇=왼쪽, △=위를 관찰하는 2D 기호 전시실 |
| `switchboard_choir` | M | 아래→왼쪽→위 순서를 직접 입력하는 2D 교환대 |
| `rain_lift` | M | 세 층을 오르내리는 3D 빗물 승강장 |
| `borrowed_title` | M | 다른 게임의 저장 화면 메뉴 위를 걷고 ○△○ 엽서를 발견 |
| `glasshouse_return` | M | 아래→위→아래로 과거 화면에 즉시 접히는 귀환 온실 |
| `teacup_orbit` | S | 보상 없이 찻잔을 일곱 바퀴 돌리는 짧은 개그 구간 |

진입은 `signal_desk` 오른쪽 보관함에서 세 번 배달한 뒤 X를 누른다. 정상 흐름은 여섯 모듈을 거쳐 `signal_desk`로 돌아오고, 온실의 숨은 출구는 `borrowed_title`로 접힌다.

## 지식 플레이 검증

1. `switchboard_choir`는 별도 해금 플래그 없이 새 상태에서 아래→왼쪽→위를 입력하면 통과한다.
2. `glasshouse_return`은 별도 해금 플래그 없이 새 상태에서 아래→위→아래를 입력하면 숨은 출구가 열린다.
3. 해법의 단서는 각각 `glyph_gallery` 표찰과 `borrowed_title` 엽서에 세계관 내 관찰로 남는다.

## 검증

- 기존 통합 러너: 639/639 유지
- GUT: 64/64, 2,758 assertions
- 신규 계약·상태·실입력 테스트: `tests/core/test_cycle2_modules.gd`
- 실제 GL 1152×720 렌더 캡처: 신규 6장면, SCRIPT ERROR/ERROR 없음
- AGENTS.md 고정 4단계: 전부 종료 코드 0

## 다음 사이클

- 첫 진입 25~26 FPS 구간의 프로파일링과 렌더 비용 절감
- 「접힌 오후」 대사·공간 밀도 확장보다 신규 세트 5~10개 폭 우선
- 지식 단서를 두 세트 이상 가로질러 재사용하는 첫 사례 추가
